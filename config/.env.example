# Environment template (copy to .env at project root when compose is ready)

# MQTT
MQTT_HOST=127.0.0.1
MQTT_PORT=1883

# Core API
API_HOST=0.0.0.0
API_PORT=8080

# Config
SITE_CONFIG=config/site.yaml

# AI
AI_ENGINE_URL=http://127.0.0.1:8090
ONNX_MODEL_DETECT=models/detect.onnx
ONNX_MODEL_CLASSIFY=models/classify.onnx

# Recorder
RECORDINGS_DIR=data/recordings
RECORD_PRE_SECONDS=30
RECORD_POST_SECONDS=60

# Database
DATABASE_URL=sqlite:///data/catalog.db
